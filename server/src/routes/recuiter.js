
const express = require("express");
const recruiterRoute = express.Router()
const Recruiter = require("../models/recruiter.model")
const Job = require("../models/job.model")


recruiterRoute.get(("/recruiter/profile",async(req,res)=>{
    try{
        
    }
    catch(err){

    }
}))